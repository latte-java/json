# Finishing Touches

Here's a list of things to do before the next release:

* [x] Add @JSONField and @JSONCatchAll tests for @JSONConstructor handling
* [x] Separate the processor into separate classes that handle: emitting helpers, validating records, validating beans, generating record companions, generating bean companions, generating polymorphic handlers
* [ ] Figure out polymorphism for classes (@JSONConstructor and Javabeans)